const apiKey = "dc59381a3c4ebdfd5cc35be53444c42d";
export const weatherApiUrl = `https://api.openweathermap.org/data/2.5/weather?appid=${apiKey}&q=${
  localStorage.getItem("search") ?? "new delhi"
}&units=metric`;

export interface CurrentWeatherResponse {
  coord: {
    lon: number;
    lat: number;
  };
  weather: {
    id: number;
    main: string;
    description: string;
    icon: string;
  }[];
  main: {
    temp: number;
    feels_like: number;
    temp_min: number;
    temp_max: number;
    pressure: number;
    humidity: number;
    sea_level: number;
    grnd_level: number;
  };
  visibility: number;
  wind: {
    speed: number;
    deg: number;
    gust: number;
  };
  sys: {
    country: string;
    sunrise: number;
    sunset: number;
  };
  timezone: number;
  id: number;
  name: string;
  cod: number;
}
